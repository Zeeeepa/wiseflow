# Core module specific dependencies
# These are in addition to the base requirements

# Include base requirements
-r ../requirements-base.txt

# No additional core-specific dependencies at this time
# If core-specific dependencies are needed in the future, add them here

